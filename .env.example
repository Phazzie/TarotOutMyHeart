# ============================================================================
# TarotOutMyHeart - Environment Configuration
# ============================================================================
#
# This file defines environment variables for local development.
# For production deployment, see `.env.production.example`.
# For CI/CD deployment to Vercel, configure variables in the Vercel dashboard.
#
# CRITICAL: Never commit actual secrets to version control!
# Use placeholder values here; actual values go in:
#   - Local machine: Copy to `.env.local` (gitignored)
#   - Vercel: Dashboard → Project Settings → Environment Variables
#   - GitHub CI: Repository → Settings → Secrets and variables → Actions
#
# ============================================================================

# ============================================================================
# REQUIRED: X.AI Grok API Configuration
# ============================================================================
#
# Your X.AI API key for accessing Grok services.
# Get your API key from: https://x.ai/api
#
# Security Note:
#   - Local development: Store in `.env.local` (never commit)
#   - Production (Vercel): Set in Vercel dashboard
#   - CI/CD: Store in GitHub repository secrets
#
XAI_API_KEY=your_xai_api_key_here

# ============================================================================
# REQUIRED: Grok Model Configuration
# ============================================================================
#
# Specify which Grok models to use for different tasks.
# These models are used by the prompt generation and image generation services.
#
# Current models:
#   - GROK_TEXT_MODEL: For analyzing images and generating tarot prompts
#   - GROK_IMAGE_MODEL: For generating tarot card artwork
#
GROK_TEXT_MODEL=grok-4-fast-reasoning
GROK_IMAGE_MODEL=grok-image-generator

# ============================================================================
# REQUIRED: Application Environment
# ============================================================================
#
# NODE_ENV specifies the application environment.
#
# Supported values:
#   - development: Local development with enhanced logging, slower load
#   - production: Optimized for speed, minimal logging
#
# Best practices:
#   - Local: development
#   - Vercel Preview: development (enables mocks for cost savings)
#   - Vercel Production: production (requires real API, higher costs)
#
NODE_ENV=development

# ============================================================================
# REQUIRED: Mock Services Toggle
# ============================================================================
#
# Controls whether to use mock services (for development/testing)
# or real Grok API services (for production).
#
# Behavior by value:
#   - USE_MOCKS=true:
#     • Uses mock services that return realistic synthetic data
#     • No API calls made to Grok
#     • Instant responses (good for development)
#     • Zero API costs
#     • Recommended for: Local development, preview deployments, testing
#
#   - USE_MOCKS=false:
#     • Uses real Grok API services
#     • Makes actual API calls to X.AI
#     • Responses depend on API availability
#     • Incurs API costs per request
#     • Recommended for: Production deployment only
#
# WARNING: Using USE_MOCKS=false in production WILL incur costs!
#          Each prompt generation call costs real money.
#          Each image generation call costs real money.
#          Monitor your X.AI account for unexpected charges.
#
USE_MOCKS=true

# ============================================================================
# REQUIRED: Public Application URL
# ============================================================================
#
# The base URL where your application is accessible to users.
# This is used for:
#   - Generating absolute URLs for sharing
#   - API callbacks and webhooks
#   - CORS configuration
#   - Open Graph metadata
#
# Recommended values by environment:
#   - Local development: http://localhost:5173
#   - Vercel Preview: https://[project-name]-[git-branch].vercel.app
#   - Vercel Production: https://tarot-up-my-heart.vercel.app
#
PUBLIC_APP_URL=http://localhost:5173

# ============================================================================
# OPTIONAL: Analytics Configuration
# ============================================================================
#
# Enable application analytics to track user behavior and performance.
# Uncomment and add your analytics provider ID.
#
# Supported providers:
#   - Google Analytics 4: GA4 measurement ID (G-xxxxxxxxxx)
#   - Plausible Analytics: Domain name
#   - Fathom Analytics: Site ID
#
# Example (Google Analytics 4):
#   PUBLIC_ANALYTICS_ID=G-XXXXXXXXXX
#
# Example (Plausible):
#   PUBLIC_ANALYTICS_ID=example.com
#
# PUBLIC_ANALYTICS_ID=

# ============================================================================
# OPTIONAL: Error Tracking Configuration
# ============================================================================
#
# Enable error tracking to monitor application stability in production.
# Recommended for production deployments to catch runtime errors.
#
# Supported providers:
#   - Sentry: Professional error tracking platform
#   - LogRocket: Session replay and error tracking
#   - Rollbar: Error monitoring and debugging
#
# Example (Sentry):
#   SENTRY_DSN=https://key@sentry.io/project-id
#   SENTRY_ENVIRONMENT=production
#
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=development

# ============================================================================
# OPTIONAL: Rate Limiting Configuration
# ============================================================================
#
# Configure server-side rate limits to prevent abuse and manage API costs.
#
# Rate limit guidelines:
#   - RATE_LIMIT_RPM: Requests per minute per IP/user
#   - Recommended for production: 60 (1 request/second per user)
#   - Adjust based on your user base and API quota
#
# Example:
#   RATE_LIMIT_RPM=60
#
# RATE_LIMIT_RPM=60

# ============================================================================
# OPTIONAL: File Upload Limits
# ============================================================================
#
# Configure limits for image uploads to the application.
# These prevent abuse and manage server storage.
#
# Guidelines:
#   - MAX_IMAGE_SIZE_MB: Maximum size per image file (default: 10 MB)
#   - MAX_IMAGE_COUNT: Maximum images per upload session (default: 5)
#   - Recommendation: Keep sizes reasonable for mobile users
#
# Example:
#   MAX_IMAGE_SIZE_MB=10
#   MAX_IMAGE_COUNT=5
#
# MAX_IMAGE_SIZE_MB=10
# MAX_IMAGE_COUNT=5

# ============================================================================
# OPTIONAL: Debug Logging Configuration
# ============================================================================
#
# Enable verbose logging for debugging application behavior.
# Useful for development and troubleshooting.
#
# Behavior:
#   - DEBUG=true: Enables detailed console logs
#   - DEBUG=false or unset: Normal logging only
#
# Warning: Don't enable in production (verbose logs impact performance)
#
# Example:
#   DEBUG=true
#
# DEBUG=false

# ============================================================================
# DEPLOYMENT INSTRUCTIONS
# ============================================================================
#
# LOCAL DEVELOPMENT:
# 1. Copy this file to `.env.local` (already in .gitignore)
# 2. Replace placeholder values with your actual API keys
# 3. Run: npm run dev
# 4. Application will respect values in `.env.local`
#
# VERCEL PRODUCTION DEPLOYMENT:
# 1. Visit: https://vercel.com/dashboard
# 2. Select your TarotOutMyHeart project
# 3. Go to: Settings → Environment Variables
# 4. Add variables for Production environment:
#    - XAI_API_KEY (actual API key)
#    - GROK_TEXT_MODEL (grok-4-fast-reasoning)
#    - GROK_IMAGE_MODEL (grok-image-generator)
#    - NODE_ENV (production)
#    - USE_MOCKS (false)
#    - PUBLIC_APP_URL (your production domain)
# 5. Optional: Configure Preview environment separately with USE_MOCKS=true
# 6. Vercel will automatically redeploy with new variables
#
# GITHUB CI/CD SECRETS:
# 1. Visit: https://github.com/yourname/TarotOutMyHeart/settings/secrets
# 2. Add secret: XAI_API_KEY (for integration tests)
# 3. GitHub Actions will use this for test runs
#
# ============================================================================

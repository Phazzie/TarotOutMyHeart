/**
 * @fileoverview Barrel export of all coordination contracts
 * @purpose Central export point for all AI coordination system contracts
 * @dataFlow Import contracts using: import type { ContractName } from '@contracts'
 * @boundary Defines all seams between coordination components
 */

// Export all coordination contracts (includes common types)
export * from './CoordinationServer'